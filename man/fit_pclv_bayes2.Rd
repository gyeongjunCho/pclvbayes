% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_pclv_bayes2.R
\name{fit_pclv_bayes2}
\alias{fit_pclv_bayes2}
\title{Pairwise Bayesian pcLV for two phyloseq objects (cross-kingdom only)}
\usage{
fit_pclv_bayes2(
  physeq_1,
  physeq_2,
  subject_col,
  time_col,
  taxa_vec_1 = NULL,
  taxa_vec_2 = NULL,
  mix_ratio = c(5, 5),
  kingdom_names = c("k1", "k2"),
  prefer_sample_order = c("phy1", "phy2"),
  ...
)
}
\arguments{
\item{physeq_1, physeq_2}{Two phyloseq objects (e.g., 16S and ITS) sharing sample IDs.}

\item{subject_col, time_col}{Columns in sample_data denoting subject and time.}

\item{taxa_vec_1, taxa_vec_2}{Optional taxa subsets per kingdom.}

\item{mix_ratio}{Length-2 numeric ratio (default c(5,5)); normalized to weights.
(e.g., c(3,7), c(7,3)). If length-1, treated as w1 and w2=1-w1.}

\item{kingdom_names}{Length-2 labels for the two kingdoms (default c("k1","k2")).}

\item{prefer_sample_order}{Which physeq decides sample ordering ("phy1" or "phy2").}

\item{...}{Other arguments forwarded to the internal pcLV fitting machinery.}
}
\value{
A tibble (same schema as \code{fit_pclv_bayes()}), with extra attributes:
\code{mix_ratio}, \code{mix_weights}, \code{kingdom_names}, \code{common_samples}.

tibble/data.frame of fits
}
\description{
This is a cross-kingdom wrapper around \code{fit_pclv_bayes()} that:
\itemize{
\item Pre-smooths each kingdom separately on \eqn{\log(\mathrm{RA}+\epsilon)}.
\item Builds triplet-ALR using a mixed composition
\eqn{p^{mix} = w_1 p^{(1)} \oplus w_2 p^{(2)}} where \eqn{w_1+w_2=1}.
\item Fits ONLY cross pairs (one taxon from each phyloseq).
}
}
\details{
Default mixing ratio is 5:5, but users can explore sensitivity (e.g., 3:7, 7:3).
}
