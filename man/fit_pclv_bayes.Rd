% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_pclv_bayes.R
\name{fit_pclv_bayes}
\alias{fit_pclv_bayes}
\title{Bayesian directed pair-to-rest fitting with irregular-time OU residuals}
\usage{
fit_pclv_bayes(
  physeq,
  subject_col,
  time_col,
  taxa_vec = NULL,
  nz_partner_min_frac = 0.15,
  min_unique_times = 3,
  min_pairs = 4,
  chains = 4,
  iter_warmup = 2000,
  iter_sampling = 2000,
  seed = 1234,
  init = 0.2,
  adapt_delta = 0.98,
  max_treedepth = 14,
  progress = c("bar", "verbose", "none"),
  n_workers_outer = 1L,
  n_workers_kfold = 1L,
  kfold_K = 5,
  kfold_R = 3,
  kfold_seed = seed
)
}
\arguments{
\item{physeq}{A \code{phyloseq} object with taxa in rows (will be transposed if needed).}

\item{subject_col}{Column name in sample metadata indicating subjects (e.g., plant/plot).}

\item{time_col}{Column name in sample metadata indicating (numeric) time within subject.}

\item{taxa_vec}{Optional character vector of taxa IDs to subset; defaults to all taxa.}

\item{nz_partner_min_frac}{Drop subjects whose partner predictor is non-zero in fewer than this
fraction of rows; default \code{0.15}.}

\item{min_unique_times}{Minimum unique times per subject; default \code{3}.}

\item{min_pairs}{Minimum valid adjacent pairs per direction; default \code{4}.}

\item{chains}{Number of MCMC chains; default \code{4}.}

\item{iter_warmup}{Warmup iterations per chain; default \code{2000}.}

\item{iter_sampling}{Post-warmup sampling iterations per chain; default \code{2000}.}

\item{seed}{Reproducibility seed for fitting; default \code{1234}.}

\item{init}{Initial value or initializer specification passed to the sampler; default \code{0.2}.}

\item{adapt_delta}{Target HMC acceptance probability; default \code{0.98}.}

\item{max_treedepth}{Maximum HMC tree depth; default \code{14}.}

\item{progress}{Progress output: \code{"bar"}, \code{"verbose"}, or \code{"none"}.
When \code{"bar"} and \pkg{progressr} is available, outer/K-fold progress bars are displayed.}

\item{n_workers_outer}{Number of parallel workers for the outer pair loop; default \code{1}.
If \code{> 1}, K-fold parallelism is automatically disabled to avoid nested parallelism.}

\item{n_workers_kfold}{Number of parallel workers for K-fold; default \code{1}.}

\item{kfold_K}{Number of folds K; default \code{5}.}

\item{kfold_R}{Number of repetitions R; default \code{3}.}

\item{kfold_seed}{Seed used only for repeated K-fold subject splits; defaults to \code{seed}. The same value is applied across all pair directions, while fold sampler seeds remain direction-specific.}
}
\value{
A list containing directional and self-effect summaries, directed
subject-level repeated K-fold ELPD, and the raw bidirectional pair table.
}
\description{
This function belongs to the \strong{pGLVbayes} framework, which extends the
classical generalized Lotka–Volterra (gLV) model to a \strong{Bayesian,
pairwise compositional (pcLV)} formulation suitable for microbiome
time-series data.

Conceptually, the gLV equations are reformulated on the additive
log-ratio (ALR) scale to handle compositional constraints, and are then
estimated in a \strong{pairwise Bayesian regression} structure with irregular-time
OU residuals. Each unordered taxon pair \verb{\{i, j\}} is fitted in
both directions (j → i and i → j), providing directed pair-to-rest dynamic coefficients that can be aggregated
across subjects; these are not generally absolute direct-gLV coefficients.

Residual dependence follows an irregular-time OU process. Student-t observation
noise and repeated subject-level K-fold Kalman ELPD scoring are fixed Core choices.

In short, \strong{pGLVbayes} generalizes the traditional gLV by introducing
(i) compositional transformation, (ii) pairwise modularization, and
(iii) fully Bayesian estimation with diagnostic-aware sampling.
}
\details{
The canonical Core uses pair-to-rest ALR predictors at lag 1 and the response
\verb{y = Delta ALR_i / Delta t}. Predictors are globally standardized using full
analysis data for the main fit and training-only statistics within each fold;
the response is not standardized.

Repeated subject-level K-fold ELPD is always computed. Held-out observations
are scored with the irregular-time OU Kalman path using a Student-t observation
likelihood with one posterior-estimated \code{nu > 2} per directed fit. Spline smoothing is selected by cross-validation.
Failed folds remain explicit and contribute no zero-valued ELPD placeholders.
The validated \code{kfold_seed} is used only to construct subject-level
split manifests and is shared across pair directions with the same subject
universe. Fold sampler seeds remain direction-specific.
\itemize{
\item A diagnostics-aware retry policy and Pathfinder initialization use the fixed
canonical v0.2 settings; their implementation controls are private.
}

\strong{Parallelism & progress}
\itemize{
\item The \strong{outer pair loop} can run in parallel with \code{n_workers_outer > 1} (PSOCK via \strong{future/furrr}).
\item \strong{K-fold} can run in parallel with \code{n_workers_kfold > 1}. To avoid nested parallelism,
when \code{n_workers_outer > 1} the effective K-fold workers are forced to \strong{1}.
\item If \strong{progressr} is installed and \code{progress = "bar"}, progress bars are shown for both outer
pairs and K-fold tasks; otherwise it falls back silently.
}

\strong{Interpretation}
\itemize{
\item For edges, prioritize \strong{posterior-supported pair-to-rest directions} (PSP/LFSR
derived from posterior draws) and \strong{MCMC diagnostics}. A sign is not by
itself evidence of direct biological facilitation or inhibition.
optionally use \strong{ELPD per subject} (from K x R) as supporting evidence. Aggregation is \strong{subject-uniform}.
}
}
\section{Progress & Parallel}{

\itemize{
\item Set global handlers once for pretty bars: \preformatted{
  if (requireNamespace("progressr", quietly = TRUE)) {
    progressr::handlers(global = TRUE); progressr::handlers("cli")
  }}
\item Reproducibility: seeds are deterministic per pair/fold (\code{seed}, \code{kfold_seed}) and
\code{furrr::future_map(..., .options = furrr::furrr_options(seed = TRUE))} is used.
}
}

\examples{
\dontrun{
if (requireNamespace("progressr", quietly = TRUE)) {
  progressr::handlers(global = TRUE); progressr::handlers("cli")
}
EP_res <- fit_pclv_bayes(
  physeq = EP_phy_obj, subject_col = "plot2", time_col = "week",
  # Pathfinder initialization is fixed by the private v0.2 policy.
  chains = 4, iter_warmup = 1000, iter_sampling = 1500,
  n_workers_outer = 4,   # outer pair loop in parallel
  n_workers_kfold = 1,   # k-fold parallel disabled when outer > 1
  progress = "bar"
)
}
#' @rdname fit_pclv_bayes
}
